language: python

python:
  - "2.7"

before_script:
  - psql -c 'create database gonibus_test;' -U postgres
  - sudo apt-get install python-xmlrunner
  - pip install psycopg2

script:
  - cd src/tests
  - python -m unittest runTests
